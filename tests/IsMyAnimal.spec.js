/**
 * Test de la fonction qui détermine si l'animal est à moi.
 * 
 * Ci-dessous vous trouverez les descriptions, en commentaire, des différentes fonctionnalités
 * que vous devez implémenter.
 * Vous pouvez organiser vos tests avec les "describe" et les "it" comme vous le souhaitez. 
 * 
 * Vos tests doivent couvrir tous les cas ! 
 * ATTENTION !
 * POUR CERTAINS CAS IL FAUT ECRIRE PLUSIEURS TESTS
 */

 //Importation de la fonctionnalité à tester
var isMyAnimal = require('../src/is-my-animal');

/**
 *  Cas 1 : La fonction doit retourner true si l'animal est un chat et false dans les autres cas
 */
describe('À MODIFIER', () => {
  it('À MODIFIER', () => {
    expect(/** À MODIFIER **/).toEqual(/** À MODIFIER**/);
  })
})
/**
 *  Cas 2 : La fonction doit retourner true aussi si l'animal est un chat, si la couleur du chat est "blanc" et false dans les autres cas
 */

/**
 * Cas 3 : La fonction doit retourner true aussi si l'animal est un chat, si la couleur du chat est "blanc" ou "BLANC" ou "Blanc" (peu importe la casse) et false dans les autres cas
 */

/**
 * Cas 4 : La fonction doit retourner true aussi si l'animal est un chat, si la couleur du chat est "blanche" ou "BLANCHE" ou "Blanche" (peu importe la casse) et false dans les autres cas
 */

/**
 * Cas 5 : La fonction doit retourner true aussi si le chat a des yeux du chat de couleur "vert" et false dans les autres cas
 */

/**
 * Cas 6 : La fonction doit retourner true aussi si le chat a des yeux du chat de couleur "vert" ou "VERT" ou "Vert" (peu importe la casse) et false dans les autres cas
 */

/**
 * Cas 7 : La fonction doit retourner true aussi si l'animal est un chien de couleur noir ou marron avec des yeux de couleur "vert" (peu importe la casse)
 */